[project]
name = "lumbra-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "transformers>=4.46.0",
    "torch>=2.1.0",
    "requests>=2.31.0",
]

[project.scripts]
lumbra-agent = "main:main"
